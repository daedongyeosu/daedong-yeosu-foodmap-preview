# 대동여수음식지도 영구 작업 지침

이 파일은 이 저장소를 다루는 모든 작업 인공지능이 채팅방과 관계없이 반드시 따라야 하는 운영 기준이다. 사용자가 같은 내용을 매번 다시 설명하게 하지 않는다.

## 신규가게 Notion 동기화

- 신규가게의 기준 원본은 Notion의 `대동여수음식지도 신규가게 등록함`이다.
- 사용자는 이 등록함 안에 신규가게를 만들고 가게마다 사진 3장을 등록한 뒤 **“홈페이지로 동기화해라”**라고 요청하면 된다.
- 같은 등록함과 이미 연결된 상위 트리 안의 가게·사진 페이지는 연결 권한을 상속한다. 사용자에게 Notion 또는 GitHub 재연결, 수동 다운로드, 파일 재업로드를 반복해서 요구하지 않는다.
- 연결된 상위 트리 밖의 새 페이지라 실제 읽기 권한이 없을 때만, 누락된 정확한 상위 페이지 한 곳의 최초 연결을 요청한다.
- 사용자의 동기화 요청을 받으면 가게 정보 확인, 사진 수집, 데이터 반영, 검증, PR, 병합, 배포, 실서버 확인까지 한 작업으로 끝낸다. 중간 과정을 사용자에게 떠넘기지 않는다.
- Notion에 올렸다는 사실만으로 홈페이지가 자동 변경되는 것은 아니다. 위 요청을 받은 작업 인공지능이 동기화를 실행해야 한다.

## 사진 규칙

- 가게마다 Notion 원본의 서로 다른 사진을 정확히 3장 반영한다.
- 고객이 가게카드/상세 화면에서 3장을 모두 넘겨 볼 수 있어야 한다.
- 대표사진 한 장만 보이게 하거나 사진을 누락·중복하지 않는다.
- 가게 사진이 아닌 메뉴판, 가격표, 영수증, 신분증, 계약서 등 민감하거나 문서형인 이미지를 임의로 가게 사진으로 사용하지 않는다.
- 사진이 3장 미만이거나 어떤 가게의 사진인지 불명확하면 추정하지 말고 그 가게만 정확히 지목하여 사용자에게 알린다.

## 기존 데이터와 기능 보호

- 신규가게는 기존 목록과 대조하여 누락된 가게만 안정적인 고유 ID로 추가한다. 중복 가게를 만들지 않는다.
- 기존 가게의 순서, 고유 ID, 가게명, 카테고리, 전화번호, 좌표, 사진, 노출 순위와 내용을 승인 없이 변경하지 않는다.
- 기존 주문앱 링크, 가게바로주문, 전화주문, 네이버지도 링크 등 모든 주문·지도 경로를 변경하지 않는다.
- 검색, 카테고리, 추천, 위치, 공유, 찜, 최근방문, 가게전용 지도, 관리자 통계 등 다른 기능에 회귀가 없어야 한다.
- 메인 `daedongmap.com`의 공통 데이터·기능 변경은 손수김밥·콩산소 등 가게전용 지도에도 동일하게 적용한다. 단, 각 가게전용 메인 슬라이드는 해당 가게 전용 구성을 유지한다.
- 요청 범위 밖의 파일과 데이터는 수정하지 않는다.

## GitHub와 운영 배포

- 운영 `main`에 직접 커밋하거나 직접 배포하지 않는다.
- 작업 브랜치 생성 → PR 생성 → 필수 검증 통과 → PR 병합 → GitHub Pages 배포 → `daedongmap.com` 실서버 확인 순서를 지킨다.
- 실패한 검증을 우회하거나 확인 전 병합하지 않는다.
- “동기화 완료” 또는 “배포 완료”라는 보고는 PR 병합과 실서버 검증까지 끝난 뒤에만 한다.

## GitHub 인증·배포 사전점검 고정 절차

이 절차는 채팅방이나 작업환경이 바뀌어도 매 GitHub 작업 시작 시 가장 먼저 수행한다. 코드를 모두 수정한 뒤에 인증 문제를 발견하여 사용자에게 재로그인·재설치를 요구하는 일을 반복하지 않는다.

1. 파일을 수정하기 전에 현재 저장소, 브랜치, 작업트리, `origin` 주소, 원격 기본 브랜치를 확인하고 최신 원격 상태를 가져온다.
2. 연결된 GitHub 앱과 로컬 Git 인증을 각각 확인한다. 브라우저 로그인, GitHub 앱 연결, `gh` 로그인, `git push` 인증은 서로 다른 인증이라는 전제로 점검한다.
3. `gh`가 없다는 사실만으로 GitHub 전체가 불가능하다고 단정하지 않는다. 현재 작업에 허용된 GitHub 앱·커넥터가 이미 연결되어 있으면 그 경로를 우선 확인한다.
4. 작업 브랜치를 원격에 올리고 PR을 만들 수 있는 인증 경로가 하나도 없을 때만 차단으로 판단한다. 이 경우에는 코드 수정 전에 정확히 부족한 인증 한 가지만 사용자에게 알린다.
5. 사용자가 이미 연결한 GitHub 앱이나 유효한 인증이 있는데도 GitHub 로그인, 저장소 재연결, 파일 재업로드를 반복해서 요구하지 않는다.
6. 인증 토큰, PAT, 비밀번호를 저장소·문서·스크립트·커밋에 기록하지 않는다. 인증정보는 플랫폼의 보안 저장소나 승인된 GitHub 앱 연결만 사용한다.
7. PR 생성 뒤 필수 검증 상태와 병합 가능 여부를 확인한다. 사용자의 명시적 병합 승인을 받은 뒤에만 병합한다.
8. preview 저장소의 병합·배포와 운영 저장소의 병합·배포를 구분한다. preview 승인을 운영 `daedongmap.com` 승인으로 간주하지 않는다.
9. 병합 뒤 GitHub Pages 완료 상태와 실제 대상 도메인의 반영 커밋을 확인한다. 확인 전에는 배포됐다고 보고하지 않는다.
10. 다른 채팅방에서 작업을 이어받더라도 이 절차와 현재 브랜치·커밋·미병합 PR 상태부터 확인하며, 사용자에게 같은 설정을 처음부터 다시 설명하게 하지 않는다.

## 표준 작업 순서

모든 변경은 `docs/PROJECT_WORKFLOW.md`의 절차를 따른다. 특히 다음 순서를 생략하지 않는다.

1. GitHub 인증·원격 상태 사전점검
2. 사용자 요청을 변경 대상·변경 금지 대상·완료 조건으로 분리
3. 최신 `origin/main`에서 작업 브랜치 생성
4. 요청 범위만 최소 수정
5. 해당 버그를 재현하고 수정 후 실패를 막는 회귀검사 추가
6. `node scripts/run-regression-suite.mjs`와 필수 정적·데이터 검사 실행
7. PR 템플릿을 빠짐없이 작성하고 필수 검증 확인
8. 사용자 승인 후에만 병합
9. preview 배포 완료와 실제 휴대전화 동작 확인
10. 운영 반영은 별도 명시적 승인 후 진행

다른 채팅방으로 작업을 넘길 때는 `docs/HANDOFF_TEMPLATE.md` 형식을 사용하여 저장소·브랜치·커밋·PR·검증·남은 승인 상태를 빠짐없이 전달한다.

## 핵심 UX 계약과 회귀 방지

- `docs/CRITICAL_UX_CONTRACT.md`는 고객 경험의 고정 계약이다. 요청 범위가 아닌 계약 항목을 변경하지 않는다.
- 버그 수정 PR은 그 버그가 다시 발생하면 실패하는 회귀검사를 반드시 포함한다.
- 루트의 `*-regression-test.mjs` 파일은 `scripts/run-regression-suite.mjs`가 자동 발견한다. 검사를 추가하고 실행 목록 등록을 빼먹는 방식은 허용하지 않는다.
- 비공개 데이터 전환 등으로 실행할 수 없는 옛 검사는 삭제하거나 무시하지 말고, 통합 실행기의 제외 목록에 정확한 사유를 기록한다.
- 메인 슬라이드, 주문앱 복귀, 노션 광고 복귀, 가게 상세, 검색, 위치 정렬, 섬섬페이 문구, 행사 팝업은 관련 없는 PR에서도 보존 검사 대상이다.
- 고객 화면을 바꾸는 PR은 390×844 모바일 기준으로 실제 브라우저 검사를 수행한다.

## 보정 코드 관리

- 새로운 `rc8-fixes.js`처럼 보정 레이어를 계속 추가하지 않는다.
- 기존 기능을 런타임에서 여러 번 덮어쓰는 수정은 최소화하고, 가능한 경우 해당 기능의 소유 파일에서 직접 수정한다.
- 현재 소유 기준은 기본 렌더링 `app.js`, 주문앱 복귀 `rc2-fixes.js`, 메인 슬라이드·위치 정렬 `rc6-fixes.js`, 주소 지도 `rc7-address-map.js`, 가게 혜택 `store-service-info.js`, 메뉴 미리보기 `store-menu-preview.js`다.
- 여러 보정 레이어를 한 번에 전면 개편하지 않는다. 고객 기능을 유지하면서 회귀검사를 먼저 추가한 뒤 기능 단위로 단계적으로 통합한다.
- 같은 문제를 두 파일 이상에서 고쳐야 한다면 PR 설명에 각 파일을 건드린 이유와 최종 소유 파일을 적는다.

## 완료 전 필수 검증

- 기존 가게 전체가 작업 전과 정확히 동일한지 비교한다.
- 신규가게 수와 이름이 Notion 원본과 일치하는지 확인한다.
- 각 신규가게의 사진이 정확히 3장이고 서로 다르며, 배포 URL에서 모두 HTTP 200으로 열리는지 확인한다.
- 기존 주문·지도 링크, 목록 순서와 핵심 기능이 보존되었는지 확인한다.
- 실제 브라우저에서 검색 → 가게 열기 → 사진 3장 이동을 확인한다.
- PR 필수 검증이 모두 통과했는지 확인한다.
- 병합 뒤 운영 도메인의 데이터와 화면에서 동일하게 확인한다.
