name: Prepare Legacy Menu Asset Restore

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/restore-legacy-menu-assets.yml

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate trusted installer file
        run: echo "Legacy menu asset restore installer is ready"
